(function($) { // => Make sure dom ready before call the script
    $(function() { // => Execute the script
        // js / jQuery script
        console.log("This is from jQuery");
    });
})(jQuery);